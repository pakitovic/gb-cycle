# Test fixtures

Phase `0` reserves this directory for artifacts consumed by automated
`gb-core` tests.

- `roms/`: input ROMs or synthetic cartridge images used by harnesses and
  integration tests
- `traces/`: golden trace artifacts used to lock debugger and timing-visible
  output

Keep fixtures deterministic and suitable for automated CI usage.
