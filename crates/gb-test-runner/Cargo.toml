[package]
name = "gb-test-runner"
version = "0.1.0"
edition = "2024"
rust-version = "1.93"
license = "MIT OR Apache-2.0"

[dependencies]
gb-core = { version = "0.1.0", path = "../gb-core" }
serde = { version = "1.0", features = ["derive"] }
sha2 = "0.10"
toml = "0.8"
