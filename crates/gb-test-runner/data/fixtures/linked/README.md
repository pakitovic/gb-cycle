# Linked-session fixtures

This directory stores repo-owned synthetic fixtures for linked-session validation
in `gb-test-runner`.

The initial `dmg04/` set contains two minimal redistributable ROMs plus a
retained combined trace fixture for a deterministic two-console `DMG-04`
exchange. These fixtures are intended to validate the linked-session harness,
not to stand in for commercial game workflows.
