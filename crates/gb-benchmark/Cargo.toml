[package]
name = "gb-benchmark"
version = "0.1.7"
edition = "2024"
rust-version = "1.93"
license = "MIT OR Apache-2.0"

[dependencies]
gb-core = { version = "0.1.7", path = "../gb-core" }
serde = { version = "1.0", features = ["derive"] }
toml = "0.8"
